# 2026-04 Plans

## Monthly Feature Checklist
- Update this file before implementation, during meaningful scope changes, and after verification.
- Missing monthly checklist updates mean the task is not complete.
- [x] 2026-04-15 - bootstrapper-embedded-skill-unification - implementation summary (`implemented-verified`)
  - Doc: `plans/2026-04-15-bootstrapper-embedded-skill-unification.md`
  - Trace: vendored the installed `agent-governance-scaffold` skill into `skills/agent-governance-scaffold`, upgraded the embedded templates and bootstrap script for portable repo-relative paths and the ideas workflow, and added repo-root wrapper, alignment, and local-skill update commands
  - Trace: re-bootstrapped the Bootstrapper repo root from the embedded skill so the root scaffold is now self-hosted from the same canonical source used for downstream target repos
  - Review: `./scripts/bootstrap_agent_governance.sh --check-updates /home/ethan/Bootstrapper` passed, `./scripts/check-bootstrapper-alignment.sh --target /home/ethan/.config` confirmed repo-root and local-skill alignment, and `.config` is now detected as a behind downstream consumer instead of silently drifting
- [x] 2026-04-15 - bootstrapper-local-drift-merge - implementation summary (`implemented-verified`)
  - Doc: `plans/2026-04-15-bootstrapper-local-drift-merge.md`
  - Trace: recovered the omitted March existing-folder workflow into the repo history, restored monthly-plan repair logic in the embedded bootstrap script, and added a compatibility wrapper for the older apply command name
  - Trace: refreshed the installed local Codex skill from the repo clone and confirmed the alignment checker now cleanly separates local-skill alignment from the still-behind `~/Bootstrapper` copy
  - Review: syntax checks passed; temp bootstrap/apply tests repaired incomplete monthly plans; `./scripts/check-bootstrapper-alignment.sh --target /Users/ethan/Bootstrapper` now reports local-skill OK and flags only the older local Bootstrapper drift

## Implementation Docs Created
- Record whether each item is `implemented-verified`, `partial`, or `not-implemented`.
- [x] `plans/2026-04-15-bootstrapper-embedded-skill-unification.md` - full implementation plan/output copied from the agent response (`implemented-verified`)
- [x] `plans/2026-04-15-bootstrapper-local-drift-merge.md` - full implementation plan/output copied from the agent response (`implemented-verified`)
