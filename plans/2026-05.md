# 2026-05 Plans

## Monthly Feature Checklist
- Add one checklist entry per significant feature implementation.
- Each entry should include:
  - the implementation date
  - a short summary
  - status such as `planned`, `in-progress`, or `implemented-verified`
  - a `Doc:` line pointing to `plans/YYYY-MM-DD-feature-name.md`
- Update this file before implementation, during meaningful scope changes, and after verification.
- Missing monthly checklist updates mean the task is not complete.
- [x] 2026-05-19 - recursive-self-bootstrap-and-templates-sync - dogfood the scaffold inside the skill folder + add `sync-templates-from-self.sh` so `assets/templates/*` stays aligned with root governance (`implemented-verified`)
  - Doc: `plans/2026-05-19-recursive-self-bootstrap-and-templates-sync.md`
  - Trace: pre-change review recorded; downstream Codex/Claude deploys identified; edge cases enumerated
  - Review: post-change continuity review complete; deploy via `update-local-codex-skill.sh` clean
- [x] 2026-05-19 - plans-as-reference-and-strict-verifier - extend `verify-plans-workflow.sh` with INDEX↔disk drift detection + add "Prior Implementation Context" rule wired into CLAUDE/AGENTS/.codex/PROJECT_RULES (`implemented-verified`)
  - Doc: `plans/2026-05-19-plans-as-reference-and-strict-verifier.md`
  - Trace: pre-change review recorded; cursor/claude rule pair kept byte-identical; downstream targets identified
  - Review: strict verifier surfaced expected drift in InvoiceTracker-IS and in this repo (until INDEX updated); all sync + verify commands exited 0
- [x] 2026-05-19 - scaffold-version-bump-and-bootstrapper-port - bump SCAFFOLD_VERSION to 2026-05-19.1, port edits back to Projects/Bootstrapper as canonical source-of-truth, propagate via --auto-update to all scaffolded target repos (`implemented-verified`)
  - Doc: `plans/2026-05-19-scaffold-version-bump-and-bootstrapper-port.md`
  - Trace: pre-change review recorded; downstream dependents identified (codex/claude deploy targets + 3 scaffolded repos); edge cases enumerated
  - Review: all three targets bumped to 2026-05-19.1; TripInvoiceTracker verifier clean; InvoiceTracker-IS and InvoiceTracker-SJ verifier surfaced pre-existing INDEX↔disk drift (18+1 and 1+1 respectively) — drift is real workflow violations the new strict verifier was designed to catch, not regressions from this change
- [x] 2026-05-21 - bootstrap-cli-launcher - add `scripts/install-bootstrap-command.sh` that installs a global `~/.local/bin/bootstrap` launcher (mirrors the `aip` convention) thin-wrapping the canonical scaffold script against `$PWD` (`implemented-verified`)
  - Doc: `plans/2026-05-21-bootstrap-cli-launcher.md`
  - Trace: pre-change review recorded; canonical-script call chain traced; no downstream bootstrapped repos affected (installer is repo-root only); edge cases enumerated
  - Review: bare `bootstrap` scaffolded a fresh temp dir and preserved a pre-existing file; `--check-updates` reported up to date; sync + plans verifiers exited 0
- [x] 2026-05-29 - bootstrap-init-update-flags - add `--init` as an explicit synonym for install mode in the canonical scaffold script and document the already-existing `--update`; keep the `bootstrap` command name (user declined rename to `bootstrapper`) (`implemented-verified`)
  - Doc: `plans/2026-05-29-bootstrap-init-update-flags.md`
  - Trace: prior `bootstrap` launcher conflict surfaced to user via question; call chain traced (launcher -> repo-root wrapper -> embedded arg parser/MODE dispatch); only the embedded canonical script changed; no downstream repo behavior change; local Codex skill drift until explicit refresh noted
  - Review: `--help` lists `--init`; `--init <tmp>` produced full scaffold and `--check-updates` reported up to date; bare call still installs; live `bootstrap --init` worked with no reinstall (passthrough)

## 2026-05-19 - Governance Bootstrap

### Pre-Change Trace (Gate A)
- [ ] Identify affected files and dependents.
- [ ] Enumerate edge cases.
- [ ] Record integration points and downstream consumers.

### Plan
- [ ] Implement requested changes.
- [ ] Validate integrations.
- [ ] Record verification outcomes.

### Optimization / Improvement Additions
- [ ] Add future improvements discovered during implementation review.

### Post-Change Continuity Review (Gate B)
- [ ] Re-trace modified paths.
- [ ] Verify signatures/imports/dependents.
- [ ] Record verification commands and outcomes.

## Implementation Docs Created
- Add one entry for each dated implementation document created this month.
- Record whether each item is `implemented-verified`, `partial`, or `not-implemented`.
- [x] `plans/2026-05-19-recursive-self-bootstrap-and-templates-sync.md` - full implementation plan/output copied from the agent response (`implemented-verified`)
- [x] `plans/2026-05-19-plans-as-reference-and-strict-verifier.md` - full implementation plan/output copied from the agent response (`implemented-verified`)
- [x] `plans/2026-05-19-scaffold-version-bump-and-bootstrapper-port.md` - full implementation plan/output copied from the agent response (`implemented-verified`)
- [x] `plans/2026-05-21-bootstrap-cli-launcher.md` - full implementation plan/output copied from the agent response (`implemented-verified`)
