# 2026-09 Plans

## Monthly Feature Checklist
- Add one checklist entry per significant feature implementation.
- Each entry should include:
  - the implementation date
  - a short summary
  - status such as `planned`, `in-progress`, or `implemented-verified`
  - a `Doc:` line pointing to `plans/YYYY-MM-DD-feature-name.md`
- Update this file before implementation, during meaningful scope changes, and after verification.
- Missing monthly checklist updates mean the task is not complete.
- [x] 2026-09-14 - version-control-ruleset - add a `## Version Control` section to the `development-workflow` rule: branch-per-feature, pull request opened at the START of the work rather than the end, and one commit per plan task as the rollback unit, plus no-bundling, push-per-task, no force-push under review, and no self-merge without explicit delegation (`implemented-verified`)
  - Doc: `plans/2026-09-14-version-control-ruleset.md`
  - Trace: `rulesets/CONSOLIDATED_RULESET.md` is generated and must not be hand-edited; the four real sources are the repo-root and scaffold-template `.cursor`/`.claude` pairs, which `sync-agent-rules.sh` asserts byte-identical via `cmp -s`. Updating only the root pair would leave downstream projects never receiving the rule.
  - Review: `sync-agent-rules.sh --check` reports in sync; 7 files changed (4 sources, generated consolidated ruleset, 2 generated `.codex` mirrors)
- [x] 2026-09-14 - correct-chmod-defect-report - retract the defect reported alongside the ruleset change: `sync-agent-rules.sh` does NOT chmod the tree (`implemented-verified`)
  - Doc: `plans/2026-09-14-version-control-ruleset.md` (§ Correction)
  - Trace: disproved by direct test - `chmod 644` on `plans/IDEAS.md`, `README.md`, `projectbootstrapper.txt`, then running the script, leaves all three at 644 with a clean `git status`; the 67 affected files also include paths the script never writes
  - Review: root cause was pre-existing working-tree drift, attributed to the script because `git status` was not checked before starting; original cause unknown and predating 2026-09-14

## Implementation Docs Created
- [x] `plans/2026-09-14-version-control-ruleset.md` - add branch-per-feature, PR-at-start, and commit-per-task rules to the `development-workflow` ruleset and its scaffold templates (`implemented-verified`)
