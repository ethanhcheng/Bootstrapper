# 2026-09 Checklist

## Version control ruleset

- [x] `plans/2026-09-14-version-control-ruleset.md` - `implemented-verified` (added a `## Version Control`
  section to the `development-workflow` rule: branch-per-feature, PR opened at the start of the work
  rather than the end, and one commit per plan task as the rollback unit. Edited all four rule sources
  -- repo-root and scaffold-template, `.cursor` and `.claude` -- because `rulesets/CONSOLIDATED_RULESET.md`
  is generated and a hand-edit there is overwritten on the next sync. `sync-agent-rules.sh --check`
  reports in sync. Also reported a defect
  in the sync script -- later RETRACTED the same day as wrong: the script does not touch file modes.
  The 67 mode changes were pre-existing working-tree drift, attributed to the script because
  `git status` was not checked before starting. Restoring the modes was still correct; the diagnosis
  was not. See `plans/IDEAS.md`.)
